name: Release JSR

on:
  workflow_call:
    inputs:
      build:
        required: false
        type: string
        default: ''

jobs:
  release-jsr:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Setup Vite Plus
        uses: bjmhe/workflows/setup-vp@vp

      - name: Build
        run: ${{ inputs.build }}
        if: ${{ inputs.build }}

      - name: Publish to JSR
        run: vpx jsr publish
